(ns webchange.interpreter.renderer.index
  (:require
    [webchange.interpreter.renderer.animation :as animation]
    [webchange.interpreter.renderer.image :as image]))

(def animation animation/animation)
(def image image/image)
