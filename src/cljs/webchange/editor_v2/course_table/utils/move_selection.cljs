(ns webchange.editor-v2.course-table.utils.move-selection)

(defn move-selection
  [rows-data current-selection direction columns]
  (print ">> move-selection")
  (print "rows-data" (count rows-data))
  (print "current-selection" current-selection)
  (print "direction" direction)
  (print "columns" columns))


