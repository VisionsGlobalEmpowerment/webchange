(ns webchange.admin-dashboard.index
  (:require
    [webchange.admin-dashboard.button.component :as button]
    [webchange.admin-dashboard.layout.component :as layout]))

(def button button/component)
(def layout layout/component)
