(ns webchange.dev-templates.flipbook)

(ns webchange.dev-templates.flipbook
  (:require [webchange.dev-templates :as t]))

(comment
  (def course-slug "q-q-euyvnvua")
  (def scene-slug "q")
  (t/update-activity course-slug scene-slug))
