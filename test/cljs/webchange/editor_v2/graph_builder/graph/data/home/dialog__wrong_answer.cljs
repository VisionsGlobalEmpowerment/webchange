(ns webchange.editor-v2.graph-builder.graph.data.home.dialog--wrong-answer)

(def data {:audio-wrong {:data        {:phrase-text "Intenta de nuevo!"}
                         :path        [:audio-wrong]
                         :entity      :action
                         :children    []
                         :connections #{}}})
